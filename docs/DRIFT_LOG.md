# Drift log

Short entries only. See `docs/DRIFT_CHECK.md` for the procedure this follows.

Format for each entry:

```
## YYYY-MM-DD — checked by <who/what ran it>
- Checked: which checklist items
- Found: what was off, if anything
- Action: what was done about it, if anything
```

---

## 2026-09-07 — checked by this session (not fully independent — see note)
- Checked: all 7 items, ahead of first push to GitHub remote
- Found: nothing off — no hard-rule violations, STATUS.md files accurate, no contract drift, nothing un-backlogged silently, START_HERE.md chain works, comprehension matches reality, spot-checked commits match their messages
- Action: none needed. Note: not run by a fresh session as recommended — worth a real independent check once one's available

## 2026-09-08 — checked by this session (not fully independent — see note)
- Checked: all 7 items, after building the four real pipeline stages (s01-s04) and the folder rename
- Found: nothing off — no hard-rule violations (one minor note: `jsonschema` was installed while the user was away, explained afterward rather than a live approval, justified by an explicit standing "continue with s02/s03/s04" instruction), STATUS.md files match actual implementation exactly, `contracts/*.schema.json` unchanged since creation (s04_tab correctly worked around the timing question without touching them), nothing un-backlogged silently, START_HERE.md chain intact, comprehension matches reality, spot-checked commits match their messages, `__pycache__/` dirs present but correctly gitignored
- Action: none needed. Same note as last time — not run by a fresh session; a real independent check is still owed

## 2026-09-08 (later same day) — checked by this session (not fully independent — see note), ahead of a handoff to a different AI model
- Checked: git log since last check (one commit: the full-stack Next.js+Turso+s05_publish milestone), contracts/ unchanged, working tree clean, plus a broader pass specifically because this is a handoff point: cross-checked `pipeline/VERIFY.md` and `docs/DECISIONS.md`'s Checkpoint 4/5 wording against what was actually said in conversation today
- Found: `pipeline/VERIFY.md` was significantly stale (written before the DB/app existed, still listed them as "missing"); `DECISIONS.md`'s Checkpoint 4/5 trigger wording was ambiguous about whether "app" meant the local Next.js app (which now exists) or a hosted one (which doesn't yet) — the user had explicitly clarified "both processing and hosted UI" today, not yet reflected in the doc; `app/` (a whole new major piece) had no `STATUS.md` equivalent to the pipeline stages' convention; two known-but-unrecorded action items existed only in chat (rotate the Turso platform token shared in an earlier message, do a security review once the app is actually live)
- Action: rewrote `pipeline/VERIFY.md` as the actual handoff document (current true state, the one clear next step, action items, known rough edges); tightened the Checkpoint 4/5 wording in `DECISIONS.md` to the precise confirmed trigger; added `app/STATUS.md`. Same note as before — not a fresh session, a real independent check is still owed, more so now given this is a handoff to a different model entirely

## 2026-09-08 (third check, same day) — full handoff per docs/SESSION_HANDOFF.md (checked by this session, not fully independent — see note)
- Checked: all 7 drift-check items (2 commits since last check: the handoff-procedure docs, and the Vercel deployment) — both accounted for, contracts/ still unchanged, cold-discovery chain intact, backlog integrity intact, spot-checked commit matches its message. Plus, per `SESSION_HANDOFF.md`'s explicit step: re-read the session's own recent conversation for anything decided but not yet in the repo.
- Found: the app was actually deployed to Vercel and live-verified since the last check (now recorded everywhere it needed to be — `ARCHITECTURE.md`, `app/STATUS.md`, `VERIFY.md`, `PENDING_ACTIONS.md`); a security review of `app/` + `s05_publish` was run this session and came back clean, but that result existed only in chat, not the repo — a real gap this step caught.
- Action: recorded the security review result and its "re-review if the app's shape changes" caveat in `docs/PENDING_ACTIONS.md` and `app/STATUS.md`; updated `VERIFY.md`'s stale "single next step" (was still pointing at the now-completed security review) to reflect that there's no single mandated next step anymore — a real backlog choice. This is the first drift check to actually exercise `SESSION_HANDOFF.md`'s new "re-read the conversation, not just git history" step, and it found something the git-log-only checks wouldn't have.

## 2026-09-08 (fourth check, same day) — full handoff after PostHog integration (checked by a fresh Codex session)
- Checked: all 7 drift-check items; pipeline suite; app lint; Vercel production deployment and build; recent commits and contracts/ diff.
- Found: PostHog page-view analytics was completed and deployed but not yet committed; `ARCHITECTURE.md` and `VERIFY.md` still contained contradictory pre-deployment text; `s03_transcribe`'s two Basic Pitch tests fail because its CLI command omits `--save-midi` (10 passed, 2 failed). No contract drift, no hard-rule violations, and no silently implemented backlog items.
- Action: documented the minimal PostHog configuration and deployment, corrected the stale deployment claims, and marked the Basic Pitch regression as the next required pipeline task. The pipeline code was deliberately not changed during this handoff. Basic Pitch also left an untracked 51-byte `:memory:.ses` artifact at the repo root; it is not part of this commit and remains until the user explicitly approves its deletion or ignoring it.

## 2026-09-08 (fifth check, same day) — full handoff per `docs/SESSION_HANDOFF.md` (checked by this session, not fully independent — see note)
- Checked: all 7 drift-check items (1 commit since the last check: doc corrections only, `contracts/` unchanged, spot-checked and matches its message exactly); Quick checkpoint (12/12 pipeline tests, git in sync with `origin/master`, latest Vercel production deployment `Ready` and matching the current commit); re-read this session's own conversation for anything decided but not yet in the repo.
- Found two real things: (1) **the prior check's own "blocked by `--save-midi`" finding was itself wrong** — re-running the suite today shows 12/12 passing, and a fresh real-song run through `s01`→`s03` matched two independently-verified prior runs almost exactly. Never confirmed by re-running tests before being written down; a reminder that "found by a fresh session" isn't automatically "verified." (2) **A real production incident**, unrelated to any code change: GitHub-triggered Vercel deployments were failing (the project's Root Directory setting was unset instead of `app`) while CLI deploys kept working, masking the problem until the GitHub integration was actually relied on. Also: the `:memory:.ses` artifact has been mentioned in this log since the previous check but was never turned into an actual `docs/PENDING_ACTIONS.md` item — a drift-tracking gap in itself.
- Action: corrected `pipeline/s03_transcribe/STATUS.md` and `pipeline/VERIFY.md` (reverted "blocked" → "done", left a visible correction note rather than silently rewriting); fixed the Vercel Root Directory setting and recorded the full incident in `app/STATUS.md`; fixed a stale line in the root `CLAUDE.md` still claiming "nothing under `pipeline/` is implemented"; added the `:memory:.ses` decision and the Root Directory fix to `docs/PENDING_ACTIONS.md`; reworked `docs/SESSION_HANDOFF.md`'s Full handoff section to explicitly enumerate today's major pieces (pipeline stages, `app/`, the database, and deploy config — the last of which is exactly what just drifted silently) and to require an explicit ask, not a silent skip or a silent run, before three expensive/state-changing actions: a real audio-pipeline walkthrough, pulling full Vercel build logs, or triggering a new deployment. Per that new rule: none of the three were needed this time (pipeline code unchanged since its last real verification today; latest deployment already `Ready` and current) — noted rather than silently skipped.

## 2026-09-09 (sixth check) — full handoff per `docs/SESSION_HANDOFF.md`, at the end of a large `app/` UI session (checked by this session, not fully independent — see note)
- Checked: all 7 drift-check items. 5 commits since the last check (design-token playground, fretboard diagram v1 + its tight-fit revision, Vercel auto-deploy-off, the CLI-deploy-workflow fix) plus a large batch of *uncommitted* work being handed off in this same check (Sheet view, an independent metronome, tabbed song page, tuning display fix, a real layout bug fix). `contracts/` unchanged. Spot-checked two of the more complex commits (`db09492`, `79cc41a`) file-by-file against their messages — exact match, no scope creep. Cold-discovery chain (`START_HERE.md`/`AGENTS.md`/`docs/GUIDE.md`/`docs/ARCHITECTURE.md`) confirmed untouched this session. Pipeline suite still 12/12, app suite 19/19, lint clean.
- Found: `app/STATUS.md`'s own top-line `Status:` summary was stale — still described the song page as rendering only the ASCII tab, after several sessions' worth of edits had added a tabbed Sheet/Fretboard/Ascii UI and a metronome without ever revisiting that one summary line. `pipeline/VERIFY.md` had two smaller staleness issues: it listed "playback/metronome/note-highlighting" as an open backlog item that's now actually built (for Sheet), and a note claiming `.claude/launch.json` lives outside the repo — checked directly, it's a real, git-tracked file inside the repo, that claim was simply wrong (not something that broke this session, just never verified before being written).
- Action: fixed `app/STATUS.md`'s top summary line; updated `pipeline/VERIFY.md`'s backlog framing and corrected the `.claude/launch.json` claim; the large `app/` feature batch itself was already documented incrementally as it was built (component `RULES.md` files, `app/STATUS.md`, `docs/DECISIONS.md`) rather than written up after the fact — this check mainly caught the *summary-level* drift that incremental per-feature documentation doesn't, by design, self-correct. `docs/PENDING_ACTIONS.md` unchanged — nothing new for a human to do surfaced (the metronome is pure client-side state, no new write path, so the standing "re-review if the app's shape changes" security trigger is not considered tripped). Committing and pushing everything as part of this handoff, per explicit request ("I want everything to be understood by the next model"). Same standing note as every prior entry: not a fresh/independent session.

## 2026-09-09 (seventh check) — handoff after the app was actually deployed live, and the docs/decisions + app/status split (checked by this session, not fully independent — see note)

- Checked: git sync (local matches `origin/master` at `c864a4e`, nothing new to push before this check began), `contracts/` unchanged since the last check, both test suites still green (12/12 pipeline, app suite passing), `docs/PENDING_ACTIONS.md` still accurate. Deliberately lighter than a full 7-item pass, given the context budget for this session was nearly exhausted — see "flagged improvement" note below.
- **First real use of the new `docs/decisions/`/`app/status/`/`docs/audio-tools/` structure for an actual handoff, not just its own construction.** Worked as intended: no need to re-read the large original files (they no longer exist in that form), and reasoning about "is anything stale" didn't require paying the cost of a 5,452-word file — most of what needed checking was already fresh in-session from having just written it.
- Found: the app's Sheet/Fretboard/metronome work — committed in the last check but explicitly *not yet deployed* at that time — has since actually been deployed live and verified on the real production URL. `pipeline/VERIFY.md` still said "not yet deployed," and `docs/PENDING_ACTIONS.md` still pointed at the pre-split `app/STATUS.md` for the Root Directory incident instead of its new home, `app/status/deployment.md`.
- Action: corrected both. Added one new pending item: the case-study working files (`Case study notes.md`, `case study prmpting.md`, both intentionally gitignored) exist in exactly one place — this Mac — worth a personal backup given how much real evidence now lives only there, without ever putting them in the public repo.
- **Flagged for the user as a potential improvement, not acted on:** this session ran very long (deploy diagnosis → full UI build → documentation restructure → case-study prep) inside one continuous context window, which is why this final handoff had to be deliberately lighter-weight than the full drift-check procedure calls for. A natural place to have split this into a fresh session was right after the documentation restructure shipped — a clean, self-contained stopping point — rather than continuing straight into new feature work and case-study material in the same context. Worth considering for future long work sessions: treat "a real architectural milestone just landed and was verified" as a deliberate handoff point, not just a place to keep going, so the *next* piece of work starts with a full context budget instead of an already-strained one.

## 2026-09-09 (eighth check) — handoff after building the backlog system, docs-only session (checked by this session, not fully independent — see note)

- **Scope note:** this entire session touched only `docs/` — no pipeline or `app/` code changed — so this is a lighter check by nature, not a shortcut taken. **Pipeline pytest was explicitly skipped per the user's own request** ("handover without any of the audio processing tests"), not silently assumed still-passing — the last confirmed result remains the seventh check's, unchanged since.
- Checked: git sync (local matches `origin/master` exactly at `aed4e9f`, nothing to push before this session's own uncommitted work), `contracts/` unchanged, app test suite still 19/19 (pipeline suite not run, see above), latest Vercel production deployment `Ready` and consistent with the current commit (11h old, no reason to pull logs), `docs/PENDING_ACTIONS.md` skimmed — nothing overdue beyond what's already listed. Re-read this session's own conversation for anything decided but not yet in the repo.
- **What this session actually built:** `docs/BACKLOG.md` — a new, fourth kind of doc (a working backlog, distinct from the existing why/status/rules three-homes split) consolidating 19 items from scattered locations (`docs/decisions/backlog-and-scope.md`, `ARCHITECTURE.md`'s Target design, `VERIFY.md`'s Still open list) plus new ones raised this session, each with pros/cons/effort/dependencies/related. `docs/backlog-board/index.html` — a local, single-file drag-and-drop board (no server, no build step) that generates that file, with a Light-mode visual design pulled via `npx getdesign@latest add claude` (Anthropic's own design language; verified as a real, known tool before running).
- Found, mid-session, by this session's own audit (not a separate fresh check — see caveat below): (1) a real bug — dragging a card while a category filter was active silently corrupted the order/position of unrelated hidden cards, in the worst case scattering them to the very end of the whole board; (2) a real maintainability trap — the tool always preferred a browser's saved state wholesale over the embedded seed data, meaning any future edit to that seed (like adding a backlog item) would be invisible to an already-used browser, with "Reset to seed" as the only fix — and that destroys all prioritization work first; (3) several items' `Dependencies`/cross-reference text referenced each other by position number (`#12`, `#10`, `#9`), which silently went stale the moment a new item was inserted in the middle, proving the exact fragility (1) and (2) were about; (4) two documentation-completeness gaps — `docs/backlog-board/` wasn't mentioned anywhere in `docs/GUIDE.md`'s map, and the new tool had no `STATUS.md` despite the project's own convention that major pieces get one.
- Action: fixed the filter/drag bug and the seed-merge trap directly in `index.html` (both re-verified afterward via direct function calls in a live browser session, not just code review); converted every numeric cross-reference to a title-based one; added defensive input validation (`normalizeItem`) so a bad category/status/priority/import can't render as `undefined` or break the board; added a "Copy Markdown" button and an open-question badge on cards as small usability wins along the way. Added `docs/backlog-board/STATUS.md`; added the `docs/GUIDE.md` map line; added one new `docs/PENDING_ACTIONS.md` item — whether this tool + its `DESIGN.md` should be public on GitHub, an assumption this session proceeded on but was never given an explicit final yes.
- **Caveat specific to this entry:** the mid-session "audit" above was requested by the user and performed by this same session, immediately after building the thing being audited — not the independent fresh-session review this document's own header recommends, and arguably an even sharper version of that gap than usual, since the auditor and the builder share every assumption. The bugs found were real and independently verified in a live browser, which is reassuring, but a genuinely fresh session re-reading `docs/backlog-board/index.html` cold is still worth doing, more so than most prior entries' "not fully independent" notes, given no one outside this session's own context has looked at this code yet.

## 2026-09-09 (ninth check) — CodeScene MCP audit + relocation follow-up, docs-only session (checked by this session, not fully independent — see note)

- **Scope note:** project relocation happened this session too (`/Users/tomsvarpins/Documents/Guitar APP` → `/Users/tomsvarpins/Projects/guitar_tab_processor`, permission/iCloud-sync issues) — already fully recorded in `pipeline/VERIFY.md` and `docs/PENDING_ACTIONS.md`, not repeated here. This entry covers the CodeScene audit and its follow-on documentation changes only.
- Checked: git sync (local matches `origin/master` at `af978b9`), pipeline suite still 12/12 after the relocation's `.venv` rebuild, `contracts/` unchanged, `docs/PENDING_ACTIONS.md` current.
- **What happened:** set up CodeScene MCP (manual `.mcp.json`, since `/plugin marketplace add` doesn't work in this desktop-app environment), ran a standalone-tier Code Health baseline over `app/` + `pipeline/` (26 files) — everything Green or perfect, four real minor complexity findings, one already-documented-and-dismissed finding (`publish.py`'s `_load_env`). Recorded in new `docs/codescene/STATUS.md` + `PROCEDURE.md` (next run targeted ~2026-09-18/19, before the trial expires 2026-09-21). Added two backlog items (the four-function cleanup; cleaning up the CodeScene integration once the paid service lapses) and sharpened an existing one. Extracted the underlying discipline into `docs/decisions/stack-and-tooling.md`, deliberately stated tool-independently so it doesn't depend on CodeScene remaining available. Added a new `docs/DOCUMENTATION_PRINCIPLES.md` section ("Writing for token-efficient navigation") after checking real word counts across every topic-file folder — confirmed the current 2-level indexing depth is still correct for this project's size, no 3rd level warranted.
- Found, mid-session: the backlog-board sync workflow (edit the board → open it in a browser → regenerate the whole `BACKLOG.md` via JS → write the whole file back) was being used for single-item additions, where it isn't needed — reads and rewrites an entire ~20KB document through the model for a one-line change. Real, self-inflicted token waste, caught only because the user was watching usage and asked why.
- Action: switched to direct, targeted edits on both `index.html`'s `SEED` and `docs/BACKLOG.md` for routine additions; documented the distinction (targeted edits for AI-driven additions, the full browser round-trip only after a real user drag-and-drop reorder) in `docs/backlog-board/STATUS.md` so this doesn't get relearned next time. Same standing note as every prior entry: not a fresh/independent session — the CodeScene findings especially would benefit from a second look, since the auditor (this session) also wrote the principles derived from its own findings.

## 2026-09-11 (tenth check) — full handoff after adopting engineering process end-to-end (checked by this session, not fully independent — see note)

- **Scope note:** this was an unusually long, unusually consequential session — process/tooling adoption (Superpowers, cherry-picked ideas from Ponytail/Karpathy-derived sources), a full documentation de-bloat pass, real CI + branch protection + Dependabot going live, an interactive diagram tool wired into the design system, and portfolio case-study notes — not a single narrow change. **The ninth check's own closing note recommended treating a landed architectural milestone as a deliberate handoff point instead of continuing in the same context — that advice wasn't followed this time either**, for the same reason as before: momentum. Worth repeating again, not just noting once: the next real milestone (CI going green, say) is a good place to actually stop, not just a good place to remark that stopping would have been good.
- Checked: pipeline suite 12/12, `app/`'s `npm run verify` PASS, git sync (local matches `origin/master` at `612c74b`), `contracts/` unchanged, 3 Vercel Production deployments `Ready` (~7h old — not correlated to a commit, out of scope for a routine check), `docs/PENDING_ACTIONS.md` skimmed and updated, `AGENTS.md`'s hard rules unchanged by anything this session touched (checked via `git log -p`).
- **What happened, briefly (each already has its own STATUS/decision file — not re-derived here):** adopted the Superpowers plugin as the project's process driver, cherry-picked two external ideas (a YAGNI decision ladder, an assumption-surfacing rule) rather than installing their source plugins — `docs/decisions/agent-workflow-tooling.md`. Trimmed real duplication out of `AGENTS.md`/`DECISIONS.md`/`START_HERE.md`/`CLAUDE.md`/`pipeline/VERIFY.md`, deleted two confirmed-dead files. Built and — after real trouble — actually landed working CI (`app` job, live since this session, took 4 real fixes to get green: a missing OAuth scope, an uncommitted `.nvmrc`, a too-narrow path filter, and `verify.sh`/the pre-commit hook itself having been built and tested locally but never pushed). Turned on branch protection for real (a GitHub ruleset, PRs now required for `app/` work — used for the first time this session, `#5`, merged clean). Reviewed and resolved all 4 open Dependabot PRs (2 merged, 2 closed with the specific real incompatibility named). Built `docs/patch-bay/` — two interactive, animated diagrams (development workflow, pipeline architecture) with real click-to-detail content and a guided trace mode — and wired its palette into `app/design_system/` as a proper third theme (`patchbay`), fixing a stale comment left over from an earlier deletion along the way.
- **Found, mid-session, worth its own line:** `.nvmrc`, `app/scripts/verify.sh`, `app/package.json`'s new scripts, and `.githooks/pre-commit` had all been built and genuinely tested *locally* several turns earlier in this same session, then never actually committed — invisible because they worked regardless of git status. Same root cause, four separate times. The lesson taken forward: after building anything CI depends on, confirm `git status` is clean before calling it done, not just that it runs.
- **Left mid-build, deliberately paused, not broken:** a third `docs/patch-bay/` diagram (technology/tech-stack architecture — local pipeline / application / data & hosting / source control, as a hub around the database rather than a straight line) was one CSS edit into being built when this handoff was requested. The file is in a fully working state — both existing tabs still function, the added CSS classes are just unused so far. Resume by adding the third tab button, its stats, the `<figure id="fig-stack">` SVG block, and wiring it through the existing generic `setupDiagram()` — the plan (card layout, coordinates, content, the 3 real relationships to draw) was already fully worked out in-conversation before the pause.
- **Found, not this session's to fix:** a concurrent Claude Code session has substantial uncommitted work across `app/`'s component tree (Fretboard, Metronome, Sheet, a new `AppHeader`, a new `StringOrientationToggle`) — noticed via `git status` during this checkpoint and confirmed via an independent hook signal ("another chat's dev server is running in this folder"). Not touched, reviewed, or committed by this session; flagged in `docs/PENDING_ACTIONS.md` since only the user can see both sessions to decide what to do with it.
- Action: `docs/PENDING_ACTIONS.md` updated with the above plus a note that the "deploy the home page" item may already be stale given the 3 recent `Ready` deployments (unconfirmed, not assumed). Same standing caveat as every prior entry, sharper this time given the sheer scope: this is a self-check, not an independent one, and a genuinely fresh session reading the new CI/process setup cold — not just the code it produced — is worth doing before trusting it as thoroughly proven.
