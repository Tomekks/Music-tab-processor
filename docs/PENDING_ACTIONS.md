# Pending human actions

Things only a human can actually do — not automatable by an AI session. Checked off as done, new ones added as discovered; never delete a checked-off item, so there's a record of what's been handled. Kept separate from `docs/DRIFT_LOG.md` (records checks performed) and `pipeline/VERIFY.md` (records current technical state) — this file is specifically "what Toms needs to go do."

Format: `- [ ] <what> (<when flagged>, <why/context if not obvious>)`

- [ ] Rotate the Turso platform API token — it was pasted directly into a chat conversation on 2026-09-07. Do via the Turso dashboard (app.turso.tech), not by handing an AI the old token.
- [x] Run a security review of `app/` + `pipeline/s05_publish/publish.py` (2026-09-08) — done via the `security-review` skill, adapted from its usual "review a PR diff" framing since nothing was pending, to "audit the current live app's actual attack surface." **No findings** — parameterized queries throughout (Drizzle's `eq()`, `publish.py`'s `?` placeholders), secrets never reach client code, no `dangerouslySetInnerHTML`/`eval`, no hidden write/admin routes, standard 404 on missing songs. Full report in that day's conversation.
- [ ] **Re-run the security review once the app's shape changes** — today's clean result is specific to a fully read-only, no-auth, no-forms app. The moment a write path reachable from outside gets added (a public local-processing UI, user accounts, any form, file uploads), that's new attack surface and needs its own fresh review, not an assumption that "it was clean before."
- [x] Create a Vercel account and connect this GitHub repo (2026-09-08) — done via `vercel login` (device-code flow, user completed auth in browser) + `vercel link`, which also auto-connected the GitHub repo.
- [x] **Decide what to do with the stray `:memory:.ses` file at the repo root** (flagged in `docs/DRIFT_LOG.md` since 2026-09-08, decided 2026-09-09) — added to `.gitignore` rather than deleted (reversible). The file itself is still on disk, just no longer shows in `git status`.
- [x] Fix the Vercel project's Root Directory setting (2026-09-08) — was unset (defaulting to the repo root instead of `app`), which broke every GitHub-triggered production deployment while CLI deploys kept working. Set to `app` in the Vercel dashboard by the user; verified by redeploying the previously-failed commit cleanly. Full incident recorded in `app/status/deployment.md` (moved there when `app/STATUS.md` became an index, 2026-09-09 — see `docs/DOCUMENTATION_PRINCIPLES.md`).
- [ ] **Decide on a backup for the case-study material** (flagged 2026-09-09) — `Case study notes.md` and `case study prmpting.md` are both gitignored on purpose (never meant for the public repo), but that also means they exist in exactly one place: this Mac's disk. Given how much real prompting evidence and case-study framing now lives only there, worth a personal backup (a private doc, iCloud, a personal-only git remote) — deliberately not the public repo, just *somewhere else* too.
- [x] **Decide whether `docs/backlog-board/` (the drag-and-drop backlog tool + `DESIGN.md`) should be public on GitHub** (flagged 2026-09-09, decided same day) — yes, publish it. Committed and pushed.
- [x] **Delete the old project folder** (`/Users/tomsvarpins/Documents/Guitar APP`) (flagged 2026-09-09, done same day) — deleted once the new location was confirmed working (`.venv` rebuilt, 12/12 pipeline tests, 19/19 app tests). Re-confirmed nothing broke afterward.
- [ ] **Rotate the CodeScene access token(s)** — two were pasted directly into chat on 2026-09-09 (one turned out wrong, one is the working one). Same standing practice as the Turso token above: do via CodeScene's own account dashboard, not by handing an AI the old token.
