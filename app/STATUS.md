# app/ — the Next.js practice-tab website

**This file is an index, not the full status** — read this to get oriented and to know which topic file to open; the details live in `app/status/`. **This file must stay an index.** New status updates go into the matching file in `app/status/` (or a new one, following `docs/DOCUMENTATION_PRINCIPLES.md`) — not appended here.

**Status:** deployed and live at **https://app-six-psi-70.vercel.app**, confirmed rendering real data from the database (last deployed 2026-09-08; the tabbed UI/Sheet/metronome work is newer and not necessarily live yet — check `git log` vs. the actual deployed commit, per `app/status/deployment.md`, before assuming what's live). Reads live from the Turso database (`s05_publish` writes to it); a homepage (song list) and a per-song page with three switchable tab views (Sheet, Fretboard, Ascii) plus a metronome. See `docs/ARCHITECTURE.md` for how this fits the whole system, `docs/decisions/hosting-and-deployment.md` for why Turso/Drizzle specifically.

**Stack:** Next.js 16.3.4, TypeScript, Tailwind CSS, App Router. Node v26.3.1 / npm 11.16.0 (already on this Mac, no install needed). Auto-generated `AGENTS.md`/`CLAUDE.md` in this folder are Next.js's own version-compatibility notes (regenerated by `next dev`) — unrelated to, and don't override, the repo-root `AGENTS.md`.

**Run it:** `npm run dev` from inside `app/` (needs `app/.env.local` with `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` — already present, gitignored).

**Four separate layers, deliberately not tied to each other:** `npm run dev` (fast local iteration) → `npm run stage` (`next build && next start -p 3001` — a real production build, served locally on port 3001 so it can run alongside `dev`'s 3000; the actual "how does this behave live" check, zero cost, zero GitHub/Vercel involvement) → `git commit`/`push` (versioning — one push per finished change, not per staging check) → `npx vercel deploy --prod --yes` from the repo root (going live — explicit, costs a build, entirely separate from the above). Running a staging check doesn't require a commit first or after; push to GitHub when a change is done, not when you happen to glance at a build.

**Security review: done (2026-09-08), clean.** No findings. Specific to the app's current fully-read-only, no-auth shape — see `docs/PENDING_ACTIONS.md` for the standing reminder to re-review if that ever changes.

## Topic index

**Deployment** — Vercel setup, how to actually deploy, the Root Directory incident and how to spot it again, a known flaky (but harmless) build error. *Open if your task touches: deploying, Vercel settings, or a deploy that's failing.* → `app/status/deployment.md`

**Database and analytics** — Turso/Drizzle access and schema changes, the accepted `npm audit` issue, PostHog configuration. *Open if your task touches: the database schema, or analytics.* → `app/status/database-and-analytics.md`

**Song views and the metronome** — current state of Sheet/Fretboard/Ascii/the tabbed page/the metronome: what's built, where the code is, what's not built yet. *Open if your task touches: any of the three tab views, the metronome, or the song page layout.* → `app/status/song-views.md`

**Design system** — the local, gitignored token-tuning playground, what's adopted so far, what's planned next. *Open if your task touches: colors, spacing, or the design-system tokens.* → `app/status/design-system.md`

**Not built yet:** any local-processing UI (still CLI-only), the metronome's playhead on Fretboard/Ascii, a sequential/step-through overview mode for Fretboard, real rhythm notation, the design-system tokens applied beyond Sheet, CI.
